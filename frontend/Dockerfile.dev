FROM node:20-alpine

WORKDIR /app

# 複製 package files
COPY package*.json ./

# 安裝依賴
RUN rm -rf node_modules package-lock.json && npm install

# 複製源代碼（在開發模式下會被 volume 覆蓋）
COPY . .

# 暴露 Vite 開發服務器端口
EXPOSE 3000

# 啟動 Vite 開發服務器
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "3000"]
